#
D ^listen_addresses[[:space:]]*=
A listen_addresses = '*'

#D ^max_connections[[:space:]]*=
#A max_connections = 20

D ^unix_socket_permissions[[:space:]]*=
A unix_socket_permissions = 0700

D ^ssl[[:space:]]*=
A ssl = on
