# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             postgres        peer                    map=pgsuper
local   all             all             reject

hostnossl all           all             0.0.0.0/0               reject
hostnossl all           all             ::0/0                   reject
